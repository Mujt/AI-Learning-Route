┌──────────────┐     自然语言 Prompt       ┌──────────────┐     生成的 SQL       ┌──────────────┐
│   用户 (You)  │ ────────────────────────→ │   AI 模型     │ ──────────────────→ │  MySQL 数据库 │
│  写 Prompt    │ ←──────────────────────── │ (Claude/      │ ←────────────────── │              │
│              │     结果解读 + SQL 说明     │  DeepSeek)   │    查询结果数据       │              │
└──────────────┘                           └──────────────┘                     └──────┬───────┘
                                                                                       │
                                                                        ┌──────────────┘
                                                                        │
                                                            ┌───────────┴───────────┐
                                                            │     权限隔离设计        │
                                                            │                        │
                                                            │  ┌─────────────────┐   │
                                                            │  │  ai_readonly     │   │  → 只读账户，仅 SELECT
                                                            │  │  (日常查询用)     │   │    最安全，默认使用
                                                            │  └─────────────────┘   │
                                                            │  ┌─────────────────┐   │
                                                            │  │  ai_writer       │   │  → 写入账户，可 INSERT /
                                                            │  │  (确认后使用)     │   │    UPDATE / DELETE
                                                            │  └─────────────────┘   │
                                                            │  ┌─────────────────┐   │
                                                            │  │  root            │   │  → 管理员账户
                                                            │  │  (仅DBA持有)      │   │    绝不交给 AI
                                                            │  └─────────────────┘   │
                                                            └────────────────────────┘